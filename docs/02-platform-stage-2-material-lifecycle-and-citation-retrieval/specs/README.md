# Stage 2 Specs

实现前应先建立并确认以下规格：

- `001-single-document-ingestion-and-citation-retrieval.md`：Slice 1 的用户流程、API、状态机、失败模式、引用合同和验收。
- `002-batch-ingestion-and-cited-answer.md`：仅在 Slice 1 验收后建立，描述 Slice 2 的批量上传与带引用回答。

Spec 不是实现清单的替代品；它必须明确文件边界、可观察状态和可复验的验收命令。
